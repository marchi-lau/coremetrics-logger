<%= form_for(@tag_attribute) do |f| %>
  <% if @tag_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_attribute.errors.count, "error") %> prohibited this tag_attribute from being saved:</h2>

      <ul>
      <% @tag_attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br />
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
